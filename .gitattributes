# Large Meshy / 3D exports — use Git LFS (run: brew install git-lfs && git lfs install)
*.glb filter=lfs diff=lfs merge=lfs -text
# Optional: `git lfs track "*.obj"` for 90+ MB Luma scans (UnrealStarter/LumaScan/mesh.obj) if pushes exceed GitHub limits.
