# React Native / Expo artifacts (should not exist in Swift projects)
node_modules/
.expo/
.bundle/
android/
ios/
babel.config.js
metro.config.js
# Root package.json is used for Netlify/npm scripts (static web); do not add Expo/RN deps here.
/package-lock.json
# Expo/RN at repo root only (if reintroduced)
/tsconfig.json
app.json

# Xcode
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
xcuserdata/

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# macOS
.DS_Store
*.swp
*~

# Env (never commit secrets)
.env

# Local Supabase + Stripe public config for static web (copy from fel-public-config.example.js)
web/fel-public-config.js

# Vite React clinical gate (optional)
web/clinical-gate-react/node_modules/
web/clinical-gate-react/dist/

# Marketing site (Vite) — build output
sites/final-evolution-main-site/dist/
sites/final-evolution-main-site/.netlify/

# Optional local tooling sync folder
.fel-local-tooling/

core
node_modules
.expo

# TestFlight / altool forensic logs (may contain App Store Connect metadata)
LOGS/

# Supabase CLI — linked remote state (do not commit)
.supabase/

# Cursor / IDE
.cursor/

# Unity (if Library is generated locally)
UnityProject/Library/
UnityProject/Temp/
UnityProject/Logs/
UnityProject/obj/
UnityProject/UserSettings/

# Unreal build intermediates (if present next to templates)
**/Binaries/
**/Intermediate/
**/Saved/
**/DerivedDataCache/