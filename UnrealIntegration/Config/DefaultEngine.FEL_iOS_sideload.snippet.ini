; -----------------------------------------------------------------------------
; Merge into the project’s Config/DefaultEngine.ini for **third-party sideload**
; distribution (AltStore / ad-hoc OTA / BuildStore-style hosting). Keeps identity
; and sessions on the **Sovereign Hub** — not Apple account services at launch.
;
; Pair with repo-root manifest.plist + apps.json after you host a signed .ipa.
; -----------------------------------------------------------------------------

[/Script/IOSRuntimeSettings.IOSRuntimeSettings]
; Do not require Game Center or Sign in with Apple for provisioning / runtime.
bSupportsGameCenter=False
bEnableSignInWithAppleSupport=False
