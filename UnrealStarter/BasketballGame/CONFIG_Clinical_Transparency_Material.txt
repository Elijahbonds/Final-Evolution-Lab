Clinical_Transparency material (Muscle-and-Motion style) — Build 6
================================================================

Create Material: M_Clinical_Transparency (or Material Function MF_SkinMuscleAlpha)

1. Base: subsurface skin tone + separate muscle layer mask (UV2 or vertex color B).
2. Opacity: scalar **LayerAlpha** 0…1 (Blueprint: inhale / fascial focus).
3. Fresnel: faint rim for silhouette education.
4. **PulsePhase** scalar: multiply emissive cyan (0.2, 0.85, 0.95) along Spiral Line UVs.
5. **Congestion** scalar: lerp base color toward red (0.9, 0.15, 0.12) on thoracic + spiral regions when SFMA fail JSON is applied.

Assign MID to skeletal mesh; drive from **UFELFascialSlingVisualizer::SetSpiralPulsePhase** and **ApplySFMAHeatmapCongestion**.

Penultimate stride: animate **FrontFunctionalTension** 0→1 over last two steps; sync **UFELRhythmicCueingWidget** beat index with AnimNotify in the approach montage.
