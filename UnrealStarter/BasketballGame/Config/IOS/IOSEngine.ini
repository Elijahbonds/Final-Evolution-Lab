; Sovereign Lab — iOS Shipping / Distribution (OTA).
; Apple Team ID (Membership) — synced from Xcode project DEVELOPMENT_TEAM where set.
; For manual signing: set bAutomaticSigning=False and set MobileProvision + SigningCertificate paths.
;
; UE 5.7 uses bEnableRemoteNotificationsSupport (not bEnableRemoteNotifications).
; There is no engine key bGeneratedConfigFromProjectSettings — use bAutomaticSigning + IOSTeamID,
; or explicit MobileProvision / SigningCertificate from your Distribution profile.

[/Script/IOSRuntimeSettings.IOSRuntimeSettings]
BundleIdentifier=com.finalevolution.FinalEvoAPP
BundleDisplayName=Sovereign Lab
BundleName=SovereignLab
IOSTeamID=78L8GWA44F
bAutomaticSigning=True
MobileProvision=
SigningCertificate=
bEnableRemoteNotificationsSupport=False
bDisableHTTPS=false
FrameRateLock=PUFRL_60
bSupportsIPhone=True
bSupportsIPad=True
