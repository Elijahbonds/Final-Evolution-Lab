# API base URL used by the React app.
# Local FastAPI default:
REACT_APP_BACKEND_URL=http://localhost:8000

# Optional PayPal checkout client id. The UI loads with a sandbox/test fallback
# when this is omitted, but real checkout requires a configured client id.
REACT_APP_PAYPAL_CLIENT_ID=
