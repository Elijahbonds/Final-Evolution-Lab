# Optional for local development. If omitted, the React app uses
# http://localhost:8000 in development and same-origin in production.
REACT_APP_BACKEND_URL=http://localhost:8000

# Optional. Checkout buttons are hidden/degraded by backend configuration
# unless matching PayPal server credentials are set.
REACT_APP_PAYPAL_CLIENT_ID=
