# Start here (Windsurf / release-reference)

**This folder is the only root you were given:**  
`/Users/elijahbonds/Desktop/FEL-Release-Reference`

## What to read first (in order)

1. **`REPOSITORY_ROOT.txt`** — one line: absolute path to the Git repo clone (all source code).
2. **`MANIFEST.json`** — index with **`repo_root`** and **`absolute_path`** for critical assets.
3. **`README.md`** — table of contents for the rest of the docs in **this** folder.

## If your tool still cannot open files under the repo path

- Add the path from **`REPOSITORY_ROOT.txt`** as a **second workspace folder**, **or**
- Open that path as the project root (same machine).

This Desktop folder is refreshed by **`scripts/sync_release_desktop.sh`** in the repo (`setup-healthkit`).
