# Copy to .env in repo root (gitignored) or export in your shell.
# Obtain Client ID and Client Secret from DeepMotion (Animate 3D API access).

DEEPMOTION_CLIENT_ID=
DEEPMOTION_CLIENT_SECRET=

# Optional override (default matches SDK docs).
# DEEPMOTION_API_SERVER_URL=https://service.deepmotion.com
